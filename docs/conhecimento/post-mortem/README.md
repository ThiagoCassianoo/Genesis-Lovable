# Post-mortem

Vazio até a primeira entrega real. Formato por arquivo:
`AAAA-MM-DD-nome-curto.md` com: o que quebrou, por que, quanto tempo
levou pra detectar, a correção, e **a regra nova que impede voltar**
(e em qual agente ela foi escrita).
