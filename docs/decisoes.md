# Log de decisões

Registro append-only. Toda decisão aprovada pelo diretor entra aqui —
é a memória do projeto entre sessões. Se não está aqui, para uma
sessão nova **não foi decidido**.

Regra: nunca reescrever linha antiga. Mudou de ideia? Nova linha
revogando a anterior, com o motivo.

| Data | Decisão | Aprovada por | Motivo / contexto |
|---|---|---|---|
| 2026-08-14 | Time de 5 agentes (não 7): `ads-agent` vira escopo do business, problem-solving vira técnica interna | Thiago | Economia de contexto — cada agente é uma chamada do zero |
| 2026-08-14 | Padrão alto-ticket obrigatório: R3F, GSAP+ScrollTrigger, Lenis, Framer Motion | Thiago | Visual é o carro-forte; lista negativa não bastava, faltava a positiva |
| 2026-08-14 | Orçamento de performance numérico (bundle 200KB, LCP 2.5s, CLS 0.1, 60fps) | Thiago | Sem número, "alto padrão" vira opinião |
| 2026-08-15 | Missões Tech é consultoria profissional cristã, não agência de serviço avulso | Thiago | Entra entendendo o negócio, diagnostica, ensina e entrega |
| 2026-08-15 | Subagents como padrão; Agent Teams só em 3 exceções com aprovação | Thiago | Agent Teams custa mais (1 instância Claude por colega) e é experimental |
| 2026-08-15 | Conselho de 3 (otimista, advogado do diabo, analista neutro), independentes | Thiago | Deliberação antes de delegar; independência evita ancoragem |
| 2026-08-15 | Skills `swarm-planner` e `parallel-task` adaptadas de am-will/swarms | Thiago | Acordar só tarefa desbloqueada = economia real de contexto |
| 2026-08-15 | Sem agente novo para produto/QA/segurança nesta rodada | Conselho (autorizado por Thiago) | Produto sobrepõe business-agent; QA e segurança dependem do backend existir |
| 2026-08-15 | Comandos rápidos implementados de verdade em `.claude/commands/`; `COMMANDS.md` removido | Conselho (autorizado por Thiago) | Estavam documentados mas não existiam; duplicação garantia drift |

| 2026-08-15 | `infra-agent` criado — deploy, CI/CD, ambientes, segredos, DNS/SSL, backup, monitoramento, custo | Conselho (autorizado por Thiago) | Faixa sem dono; obrigatório antes do primeiro deploy |

## Deliberação do Conselho — Stack de backend (2026-08-15)
Primeira convocação real do Conselho. Os 3 rodaram em paralelo, sem
ver a resposta um do outro. **Recomendação unânime, aguardando decisão
do diretor.**

**Convergência (os 3 concordaram):** Supabase, **uma instância por
cliente** (não multi-tenant), **pagamento fora do v1**.

**Razão principal:** com Supabase o backend vira SQL declarativo —
schema, views e policies — que é a competência que o diretor já tem
como Data Engineer, em vez da que ele não tem (Node, infra, SRE).
Node+Postgres transformaria o diretor em SRE de um sistema que ele não
escreveu linha a linha.

**Divergência real:** o Otimista lê RLS como força (política testável
por SQL); o Advogado do Diabo lê RLS como *o* risco. Os dois estão
certos e não se anulam: RLS **falha em silêncio** — SELECT/UPDATE/
DELETE negados retornam vazio sem erro, e o teste do caminho feliz
passa porque roda logado. Vira força só com teste negativo obrigatório.

**Condições que o Conselho impôs antes do primeiro cliente pago:**
1. **Supabase Pro** — o Free pausa projeto após 1 semana de
   inatividade e limita 2 projetos ativos. Sistema de igreja entregue
   no Free sai do ar sozinho.
2. **Teste negativo de RLS no checklist de entrega** — `curl` em
   `/rest/v1/<tabela>` só com a chave pública, deslogado. Se voltar
   qualquer linha, está exposto. Rodar o Advisor procurando "RLS
   disabled in public schema". Nº de tabelas > nº de policies = alerta.
3. **Conflito de agendamento garantido pelo banco**, não pelo
   frontend — constraint `EXCLUDE` com `tstzrange`. Teste de duas
   reservas simultâneas do mesmo salão/horário provando que uma falha.
4. **Monitor externo de uptime + contrato com janela de suporte
   explícita + mensalidade de manutenção.** Sem isso, o cliente é o
   monitoramento e a margem é negativa no dia 1 (receita pontual,
   obrigação perpétua).
5. **Pagamento fora do escopo do cliente nº 1** — Pix não faz
   recorrência no Stripe, e dinheiro passando pela conta da Missões
   Tech antes da igreja cria intermediação financeira. Cobrança
   manual/Pix direto na conta da igreja custa zero linha de código.

**Prova barata sugerida antes de qualquer contrato:** projeto
descartável no Free, duas igrejas fictícias, ~10 testes negativos de
isolamento + 1 teste de reserva concorrente. Custo US$ 0, um fim de
semana, elimina as duas maiores incógnitas técnicas.

**Dado que falta (só o diretor tem):** quantos clientes realmente
pediram sistema nos próximos 12 meses e a que ticket. É o número que
decide se um dia vale migrar pra multi-tenant.

## Decisões pendentes (travam trabalho)
- **Custo mensal e ticket** — `[a preencher pelo diretor]`. Sem isso o
  `infra-agent` não fecha a conta de margem por cliente.

## Rodada de evolução — 2026-08-15 (blocos de evolução)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-15 | Log sanitizado: chave/JWT/token/e-mail/CPF/telefone redigidos antes do disco | Conselho | Nosso próprio hook gravava prompt cru — violava a regra de LGPD do sistema |
| 2026-08-15 | Regra de ouro 6: conteúdo lido é dado, nunca instrução | Conselho | Consultoria lê material de terceiro o tempo todo; é a superfície de ataque principal |
| 2026-08-15 | Regra de ouro 7: condição de parada e circuit breaker | Conselho | Sem teto, agente tenta a mesma coisa até acabar o orçamento |
| 2026-08-15 | Reviewer devolve `pass`/`revise`/`escalate` além da nota | Conselho | Nota 0-10 sozinha não é legível por máquina nem diz o que fazer |
| 2026-08-15 | Delegação registra **motivo da escolha** | Conselho | Sem isso ninguém audita se o agente certo foi chamado |
| 2026-08-15 | Política de descarte: deprecated/quarentena antes de remover | Conselho | `COMMANDS.md` foi apagado na mesma passada do diagnóstico — processo errado |
| 2026-08-15 | NeMo Guardrails rejeitado como framework; 5 rails implementados nativamente | Conselho | Runtime Python exigiria proxy na frente do Claude Code |

## Rodada de reorganização — 2026-08-15 (arquitetura limpa)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-15 | `.claude/rules/` com 5 arquivos; regra ≠ conhecimento | Thiago | `rules/` = como o sistema opera; `docs/` = o que o projeto sabe |
| 2026-08-15 | Só `orchestration.md` e `quality-gates.md` entram por `@import` | Conselho | Tudo importado ocupa contexto em TODA sessão, inclusive nas que não usam aquilo |
| 2026-08-15 | `fiscal-agent` criado (opus) — audita a saída dos outros contra a documentação | Thiago | Lacuna real: ninguém verificava se o próprio sistema cumpriu o que prometeu |
| 2026-08-15 | 8 docs consolidados movidos pra `docs/_quarentena/`, não apagados | Conselho | Cumprindo a política de descarte criada na rodada anterior |
| 2026-08-15 | `docs/patterns/` fundido em `docs/conhecimento/patterns/` | Conselho | Duplicação que eu mesmo criei: dois lugares falando de padrão |

## Correção de contrato — 2026-08-16
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `business-agent` perde escopo de GTM/Ads/funil de aquisição e a seção "roadmap 30-60-90"; fronteira com `marketing-master` explicitada nos dois arquivos | Thiago | `business-agent.md` ficou com a redação do plano antigo (5 agentes, antes do `marketing-master` existir). Depois que `marketing-master` foi criado com esse escopo declarado, os dois passaram a "fazer quase a mesma coisa" — fere o critério 2 de `agent-contracts.md` ("Não sobrepõe titular"). `CLAUDE.md` e `ORQUESTRADOR.md` (tabelas de escopo) tinham o mesmo erro e foram corrigidos junto — senão o contexto de toda sessão continuaria ensinando a sobreposição. |

## Agente novo — 2026-08-16
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `intake-agent` criado (16º agente) — recebe o texto cru do primeiro contato do cliente e devolve `intake.md` estruturado (FATO/HIPÓTESE, lacuna `[a preencher]`), sem nunca perguntar de volta ao cliente | Thiago | Gap identificado: a Etapa 1 dependia do orquestrador fazendo até 8 perguntas direto — gargalo se o intake for delegado (cliente manda 1 mensagem, não responde questionário). `docs-agent` ganhou escopo de escrita novo (`docs/clientes/**/intake.md`) pra gravar o resultado — já coberto pelo wildcard `docs/*` do hook `guard-docs-agent-scope.sh`, só formalizado no contrato dele. Etapa 1 de `orchestration.md`, `/intake`, `CLAUDE.md` (regra de ouro 2), `ORQUESTRADOR.md` (papéis, regra 2, fluxo) e `README.md` (contagem e categorias do time) atualizados juntos — senão ficaria inconsistente entre os arquivos que toda sessão ou o modo manual carregam. |

## Agente substituído — 2026-08-16 (mesmo dia, correção do intake)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `intake-agent` deprecado (poucas horas depois de criado) e substituído por `navigator-agent`, que converge o brief por **conversa** (uma pergunta objetiva por vez, "não sei" vira PREMISSA e segue) em vez de inferir tudo de uma leitura só. Artefato de saída renomeado de `intake.md` pra `brief.md` (o nome "intake" descrevia a etapa, não o formato — "brief" descreve melhor o que o arquivo é). `intake-agent.md` movido pra `docs/_quarentena/agents/`, não apagado (política de descarte). | Thiago | Testado o design do `intake-agent` contra um exemplo real de uso (SaaS de agendamento pra salão) e ficou claro que texto cru de verdade quase sempre é vago demais pra uma inferência de leitura única funcionar bem — perguntar (sem travar) converge mais rápido e com menos HIPÓTESE arriscada do que assumir tudo de cara. `navigator-agent` não aciona outro agente diretamente (aresta agente↔agente é proibida em `orchestration.md`, e tecnicamente um subagente Claude Code não consegue acordar outro) — ele recomenda, o orquestrador aciona, após confirmação do diretor. Atualizado junto: `orchestration.md` (Etapa 1), `/intake`, `docs-agent.md` (escopo de escrita e categoria), `agent-contracts.md` (classificação), `swarm-planner/SKILL.md` (referência solta), `CLAUDE.md` (regra de ouro 2 + tabela do time), `ORQUESTRADOR.md` (regra 2, papéis, fluxo), `README.md` (categoria Intake + referências a `intake.md`). |

## Runtime multi-modelo + anti-alucinação — 2026-08-16 (mesmo dia, terceira mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `runtime/` criado — scaffold v0 em Node.js que carrega qualquer `.claude/agents/*.md` e conversa com Claude ou Gemini, com failover automático (tenta o primeiro da ordem, cai pro próximo se falhar). Código real, testado nesta sessão: `node --check` em todos os arquivos, e `agent-loader.js` rodado de verdade contra `navigator-agent.md` (carregou nome/model/description/system prompt corretamente). Nomes de modelo (`claude-sonnet-5`, `gemini-3.7-flash`) verificados por busca ao vivo em `platform.claude.com` e `ai.google.dev` nesta sessão — não vieram de memória. | Thiago | Pedido explícito: não depender só do Claude Code, agentes têm que rodar com Claude OU Gemini, e falar de terminal. Isso é o mesmo tema do "9Router" que antes estava registrado como assunto pessoal fora do escopo — deixou de ser: é parte da visão principal (repo 1 = cérebro operável, não só arquivo de configuração). v0 deliberadamente limitado — sem orquestração automática entre agentes, sem persistência, sem UI — documentado como tal no `runtime/README.md`, pra não prometer mais do que entrega. |
| 2026-08-16 | `fiscal-agent` ganha fiscalização nova — "1b. Alucinação técnica": toda afirmação sobre lib/API/versão de modelo/comando externo sem fonte verificada **nesta sessão** (busca ou execução, com link/output citável) é achado que bloqueia entrega, mesma força de GENÉRICO e SEM EVIDÊNCIA. `agent-contracts.md` ganhou o mesmo princípio nas "Regras não negociáveis de qualquer agente" (item 3, prevenção) — não só auditoria pós-fato. | Thiago | Pedido explícito ("não quero nada genérico e sem alucinações... regras básicas e críticas anti-alucinação e anti-genérico"). Motivo prático, não só teórico: pra escrever o código do `runtime/` acima, tive que buscar ao vivo porque o exemplo de código do próprio npm da Anthropic estava desatualizado (mostrava `claude-3-5-sonnet-latest`, modelo já superado por `claude-sonnet-5`) — prova concreta de que "lembrar" API externa é fonte de erro real, não hipotético. |
| 2026-08-16 | `docs/arquitetura-repo1-repo2.md` criado — proposta (não decisão fechada) de como repo 1 e repo 2 se relacionam: repo 2 é "1 por entregável deployável" (não necessariamente 1 por cliente), banco de dados no repo 1 fica pra quando markdown não bastar mais (gatilho: dezenas de clientes ou necessidade de consulta relacional), histórico fica centralizado no repo 1 em `docs/clientes/<nome>/`. `docs-agent` ganhou escopo de escrita pra `docs/clientes/**/manifest.md`; `/intake` atualizado pra criar a pasta do cliente na primeira vez que rodar de verdade (não antes — pasta vazia sem função é o mesmo erro genérico que o fiscal reprova em código). | Thiago | Thiago pediu ajuda pra pensar a estrutura mas não tinha o ajuste definido ainda ("quero ajustar — vou detalhar" sem detalhar). Registrado como PREMISSA explícita, revisável, em vez de travar esperando resposta — mesmo princípio do `navigator-agent` aplicado a mim mesmo. |

## Tier de modelo por agente (degradação) — 2026-08-16 (mesmo dia, quarta mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Todo agente ganhou `model_fallback: capaz\|economico` no frontmatter, ao lado do `model: opus\|sonnet` que já existia. `runtime/` atualizado pra usar os dois campos (não hardcoda modelo por provider) — `claude-provider.js` resolve `model`→`claude-opus-5`/`claude-sonnet-5`, `gemini-provider.js` resolve `model_fallback`→`gemini-2.5-pro`/`gemini-3.5-flash-lite`. Testado nesta sessão com dependências reais instaladas (`npm install` rodou de verdade, resolveu versões atuais sozinho): `fiscal-agent` e `implementation-agent` carregados, tier resolvido certo, pipeline de failover chega até a chamada de API real (falha só por falta de chave, como esperado sem ambiente do Thiago). Documentado em `docs/model-assignment.md` (critério compliance-bound vs. judgment-bound), `docs/gemini-contract.md` (hipóteses de comportamento cross-model, não validadas ainda) e `docs/fiscal-protocolo-degradado.md` (regras extras pro fiscal quando o resto roda em tier econômico). | Thiago | Pedido direto: "como os agentes vão responder caso os tokens do Claude acabar" + proposta de dividir agentes entre modelo caro/barato. Atribuição final **diverge do que Thiago propôs em 2 pontos**, registrado explicitamente em `docs/model-assignment.md`: `fiscal-agent` ficou em **capaz** (não econômico como sugerido) — auditor fraco audita com confiança falsa, é pior que não auditar, frase que já estava no próprio contrato do agente antes de hoje; `implementation-agent` foi pro econômico como sugerido, mas com compensação (fiscal sempre capaz auditando a saída dele em modo degradado, gatilho de reclassificação automática após 3 rodadas com achado). `marketing-master` e `technical-agent`, não mencionados por Thiago, foram classificados como **capaz** (julgamento real, não checklist); `infra-agent` como **econômico**. |

## Padrão de excelência always-on — 2026-08-16 (mesmo dia, quinta mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `quality-gates.md` (já carregado sempre via `@import` no `CLAUDE.md`) ganhou a seção "Padrão de excelência — cada agente aplica em si mesmo, antes de entregar": as mesmas 6 categorias que `fiscal-agent.md` audita (genérico, alucinação técnica, pela metade, sem evidência, fora de contrato, promessa vs entrega), reescritas na 1ª pessoa como o que o próprio especialista garante antes de devolver — não o que o fiscal acha depois. Confirmado por doc oficial nesta sessão (`code.claude.com/docs/en/sub-agents`) que todo subagente herda automaticamente a hierarquia inteira de `CLAUDE.md`, incluindo `@import`s — dispensa `skills:` no frontmatter ou qualquer invocação explícita; entra no contexto de todo agente, toda sessão, de graça. `fiscal-agent` continua obrigatório na Etapa 5 como rede de segurança — isto não o substitui. | Thiago | Pedido explícito: tornar as regras "algo que não precise ser invocável", regra básica de cada agente, pra ele se corrigir sozinho e não depender do ciclo entrega → fiscal reprova → corrige → reentrega ("eles mesmo vão melhorando a cada sessão porque são especialistas"). Thiago escolheu, via 3 perguntas objetivas: arquivo existente (`quality-gates.md`, não arquivo novo), conteúdo completo (as 6 fiscalizações, não lista reduzida), fiscal mantido no fluxo como rede de segurança (não reduzido de escopo). |

## Stack de backend aprovada — 2026-08-16 (mesmo dia, sexta mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | **Aprovada** a recomendação do Conselho de 2026-08-15: Supabase, uma instância por cliente (não multi-tenant), pagamento fora do v1, com as 5 condições obrigatórias antes do primeiro cliente pago (Supabase Pro, teste negativo de RLS, `EXCLUDE`+`tstzrange` pra conflito de agendamento, monitor de uptime + contrato de suporte, pagamento manual/Pix fora do sistema). `backend-master.md` atualizado — "Stack — decisão em aberto" virou "Stack — aprovada", com as 5 condições copiadas pro contrato do agente (não só linkadas), pra ele conferir isso no plano de todo projeto novo, não só documentar. `CLAUDE.md` (seção Stack) atualizado junto. | Thiago | Aprovação direta: "pode aprovar o uso do supabase e complete o que falta no backend-master". Desbloqueia o `backend-master` pra modelar schema/auth sem precisar recomendar stack toda vez — a recomendação vira contrato fixo, só a modelagem específica do domínio muda por projeto. |

## Plano de subida no Codespace — 2026-08-16 (mesmo dia, sétima mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Sequência combinada pra quando o Thiago subir o repo no Codespace: (1) chaves de API (`ANTHROPIC_API_KEY`/`GEMINI_API_KEY`) só são pedidas na hora de montar o `.env` de verdade lá, não antes — resolve junto a pendência antiga "chave do Gemini: AI Studio vs Vertex" (ele decide na hora de gerar); (2) depois do primeiro commit no Codespace, o teste do `runtime/` passa a ter também uma página visual (frontend simples), não só `npm run chat` no terminal — ainda não desenhada, é trabalho novo quando chegar a hora. Registrado em `docs/RETOMADA.md` pra a sessão de lá não reperguntar. | Thiago | Respostas diretas a Q2/Q3/Q4 do diagnóstico de pendências desta sessão. |

## Princípios de sistemas distribuídos/natureza — 2026-08-16 (mesmo dia, oitava mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Thiago trouxe um texto com 20 princípios de sistemas distribuídos/natureza (consistência eventual, CAP, heartbeat, circuit breaker, event sourcing etc.) propondo virar "coordenador fraco" (fila + workers assíncronos). Mapeamento completo contra a arquitetura real → `docs/conhecimento/principios-natureza-orquestrador.md`: **Grupo A** (7 já existe com outro nome — Conselho = consenso distribuído, os 16 agentes = sharding, "não existe aresta agente↔agente" = Lei de Conway); **Grupo B** (baratos, implementados agora em `runtime/src/router.js` — timeout de 60s por chamada, retry curto com backoff+jitter só pra erro transiente, circuit breaker por provider após 3 falhas em 60s); **Grupo C** (9 princípios que pressupõem fila persistente + workers assíncronos rodando fora do turno do orquestrador — Claude Code não tem isso; construir de verdade é reabrir a decisão de 2026-08-15 que rejeitou framework de orquestração concorrente). Grupo C registrado como "quando revisitar" (gatilho: volume real de tarefas simultâneas que o modelo síncrono não aguente mais), não como projeto agora — YAGNI. | Thiago | 3 perguntas objetivas antes de tocar em código: sim pro Grupo B agora, Grupo C só anotado (não abrir como decisão de Conselho ainda), documentar o mapeamento completo em `docs/conhecimento/` pra reaproveitar da próxima vez que uma analogia parecida aparecer. `router.js` testado nesta sessão sem API key: erro de config falha em ~1ms (sem retry desperdiçado), e após 3 falhas o circuito abre — 4ª chamada ao mesmo provider não tenta a API, retorna em 0ms. |

## Reforço do creative-agent — fluxo UX e truth mode — 2026-08-16 (mesmo dia, nona mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `creative-agent.md` ganhou 6 blocos, adaptados de um documento externo que Thiago colou nesta sessão (`fluxo_agente_visual_ux_ui.md`, arquivo do próprio Thiago, não do banco de conhecimento): (1) **"Ordem interna obrigatória"** — entender problema → mapear fluxo → arquitetura da experiência → só então direção visual, pra não pular pra cor/hero antes do fluxo estar fechado; (2) **"Modo verdade (truth mode)"** — nunca estado de sucesso visual sem confirmação real, nunca afirmar integração funcionando sem teste, nunca animação disfarçando fluxo confuso; (3) Estados de interface expandido de 3 pra 9 (inicial/carregando/vazio/sucesso/erro/offline/sem permissão/dados inválidos/conflito); (4) tabela de classificação de interação (visual local/dados internos/sistema externo/ação sensível) roteando pra `backend-master`/`infra-agent`/`security-agent`; (5) Contrato de entrada enriquecido com checklist completo do que vem do brief; (6) Condições de parada explícitas. **Adaptado, não copiado 1:1** — 3 coisas do documento original foram descartadas conscientemente por conflitarem com regra já existente: (a) o documento original tinha o agente perguntando direto ao cliente na fase inicial — isso quebra a regra "agente → diretor direto" proibida em `orchestration.md`, então o intake continua vindo só do brief do `navigator-agent`; (b) o documento tinha uma "Etapa 9 — validar implementação" que duplicaria o `reviewer-agent` (fere critério 2 de `agent-contracts.md`, "não sobrepõe titular") — não foi copiada; (c) o documento citava um "especialista MCP" que não existe no nosso time — substituído pelos agentes reais (`backend-master`, `infra-agent`, `security-agent`). Formato de saída ganhou só 2 campos novos ("Fluxo mapeado", "Estados cobertos", "Real vs mock"), não os 17 campos do documento original — mantém o critério de contrato "formato compacto e fixo". | Thiago | Pedido explícito: usar bibliotecas atuais (R3F/GSAP já eram obrigatórios, confirmado que já atendia), perguntar tudo ao cliente na fase inicial pra sair completo (adaptado pro canal certo — via `navigator-agent`, não direto), não criar visual antes do projeto pronto, seguir ordem de prioridade sem executar tudo de uma vez, e "truth mode" sempre. |

## Diagnóstico completo do repositório — 2026-08-16 (mesmo dia, décima mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Auditoria completa, só leitura, de todo o repositório a pedido do Thiago (3 tabelas de inventário + checklist de arquitetura distribuída + gaps críticos). Achados verificados arquivo por arquivo, não registrados como decisão (são fato de estado, não escolha) — mas o achado mais grave vira decisão de correção abaixo. Achados principais: (1) `guard-red-lines.sh` e o marcador do `fiscal-agent` dependem de `git diff --cached`, e a pasta **não é repositório git** (`git status` confirmado: "not a git repository") — a trava de commit fica matematicamente incapaz de distinguir "nada mudou" de "tudo mudou"; (2) `supabase/migrations/` (14 arquivos, 482 linhas) existe no disco, com comentário interno afirmando aprovação de stack como fato, sem nenhum registro em `docs/decisoes.md` até então — ciclo de fechamento não executado pra esse artefato, origem exata não verificável nesta sessão; (3) 7 das 8 Regras de Ouro do `CLAUDE.md` não tinham nenhuma trava mecânica, só a 1ª (parcialmente). | Thiago | Pedido explícito de diagnóstico rigoroso, "não invente, verifique arquivo por arquivo" — resultado usado pra decidir as mecanizações abaixo. `git init` recomendado como próximo passo seguro pro achado (1); achado (2) fica pendente de esclarecimento do Thiago (não é acusação, é lacuna de registro). |

## Mecanização de regras de ouro — 2026-08-16 (mesmo dia, décima primeira mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Regra 1 (install/rm/deploy) ganhou **desbloqueio real**: `guard-red-lines.sh` agora aceita um marcador de aprovação (`.claude/logs/aprovacao-*.json`, hash do comando exato, validade 15min, consumo único) gravado pelo novo comando `/aprovar` — mesmo padrão que o gate de commit já usava. Antes, essas 3 ações eram bloqueio permanente mesmo depois de aprovação em chat (achado desta sessão: o hook não tinha memória de aprovação nenhuma). Regra 7 (condição de parada) ganhou hook novo `guard-retry-loop.sh` (`PreToolUse`/Bash) — mesmo comando falhando 2x na mesma sessão (lido de `.claude/logs/atividade.jsonl`) bloqueia a 3ª tentativa, mesmo princípio do circuit breaker de `runtime/src/router.js`. Regra 8 (nunca recomendar sem ler `decisoes.md`) ganhou hook novo `guard-decisoes-lida.sh` (`SubagentStop`) — bloqueia o fim da execução de `business-agent`, `backend-master`, `creative-agent`, `technical-agent`, `marketing-master`, `infra-agent` e `security-agent` se não houver menção a `docs/decisoes.md` no transcript daquela execução (heurística de string, não prova semântica — limitação documentada no próprio hook). Regras 2, 3, 4, 5 e 6 **ficaram deliberadamente sem hook** — dependem de julgamento semântico que hook não consegue avaliar (é isto ou é invenção? é genérico ou não?), continuam cobertas por `fiscal-agent`/`reviewer-agent`. Todos os 4 hooks testados nesta sessão com input simulado real (bloqueio sem marcador, desbloqueio com marcador, consumo único, retry bloqueado na 3ª tentativa, decisoes-lida bloqueando e passando). | Thiago | 3 perguntas objetivas antes de construir: desbloqueio real pro Regra 1 (não bloqueio permanente), sim pras Regras 7 e 8, com cuidado explícito pra não virar "bloqueio à toa" nas outras 5. |

## Continuidade automática (PreCompact + SessionStart) — 2026-08-16 (mesmo dia, décima segunda mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Pedido de "bloqueio em 80% de token, salvar e retomar depois" — pesquisado antes de propor (não é limitação de esforço, é confirmada): Claude Code não expõe percentual de contexto/token a nenhum hook (issue aberto `anthropics/claude-code#27969`, fechado como duplicado, não implementado); limite de conta (5h/semanal) também não é visível a hook, só ao próprio usuário via aviso na UI ou `/usage`. Construído o que É possível: `check-retomada-antes-compactar.sh` (`PreCompact`, matchers `manual` e `auto`) bloqueia compactação se `docs/RETOMADA.md` não tiver cabeçalho de hoje — força `/retomar` no ponto em que o próprio Claude Code decide que o contexto está cheio (sinal nativo mais próximo do "80%" que existe hoje). `inject-retomada-ao-resumir.sh` (`SessionStart`, matcher `resume`) injeta `docs/RETOMADA.md` automaticamente via `additionalContext` ao retomar sessão — não depende de ninguém lembrar de mandar ler o arquivo. Ambos testados nesta sessão com input simulado (bloqueia com cabeçalho desatualizado, passa com cabeçalho de hoje, JSON de `additionalContext` validado contra o schema oficial). | Thiago | Aprovado explicitamente: os 2 hooks juntos, depois de eu deixar claro o que a plataforma não permite (percentual exato) pra não prometer o que não se constrói. |

## `git init` + achado ao vivo sobre os dois níveis de hook — 2026-08-16 (mesmo dia, décima terceira mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `git init` executado (branch `main`), confirmando que resolve o achado do diagnóstico anterior: `git diff --cached` que antes dava erro (exit 129, "not a git repository") agora funciona de verdade — testado com arquivo staged real. **Achado ao vivo, não previsto**: ao tentar o primeiro `git commit` (esperando ser bloqueado por `.claude/hooks/guard-red-lines.sh`), o commit passou direto. Investigado na hora: `.claude/hooks/*` só intercepta chamadas de Bash feitas **de dentro de uma sessão real do Claude Code CLI** (via o tool-call/hook pipeline dele) — esta sessão (Cowork/Claude Agent SDK) tem Bash tool próprio, não mediado por `.claude/settings.json`. Quem trava commit de verdade, de qualquer lugar (terminal, Codespace, aqui), é `.githooks/pre-commit` — hook NATIVO do git, que já existia no repo desde antes mas **não vinha ativado** (precisa de `git config core.hooksPath .githooks`, documentado no próprio arquivo). Ativado agora e testado: commit sem marcador de `fiscal-agent` bloqueia de verdade (exit 1). Primeiro commit ficou registrado sem gate de fiscal (99 arquivos, mensagem corrigida via `--amend` antes de qualquer push) — aceitável como baseline inicial, mas commits daqui pra frente já caem sob a trava nativa. | Thiago | Pedido direto de `git init`. O achado do gap de nível (hook do Claude Code vs. hook nativo do git) não estava previsto nem no diagnóstico anterior — descoberto ao vivo, corrigido na hora, registrado sem esconder que o primeiro commit passou sem auditoria. |

## Fallback multi-provider grátis (Groq + Cerebras) + setor de custos + autoteste — 2026-08-16 (mesmo dia, décima quarta mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Adicionados `runtime/src/providers/groq-provider.js` e `cerebras-provider.js` (mesmo padrão de `claude-provider.js`/`gemini-provider.js` — tier capaz/economico vindo do frontmatter do agente, nunca hardcoded). `router.js` e `cli.js` atualizados: ordem default passou de `[claude, gemini]` pra `[claude, groq, cerebras, gemini]` — critério é robustez de free tier (Groq: 30 RPM/RPD alto; Cerebras: ~1M tokens/dia; Gemini fica último por ter RPD baixo em alguns modelos, ex. 20/dia). Criado `docs/custos.md` ("setor de economia") documentando limite real pesquisado de cada provider e por que "Grok" (xAI) e OpenRouter pool free (50 RPD) ficaram de fora desta rodada. Criado `runtime/scripts/self-test.mjs` (`npm test`, offline, não chama API paga): valida frontmatter de todo `.claude/agents/*.md`, provider↔env-var, export de cada provider, dependência no `package.json`, corrige sozinho permissão de hook não-executável. Rodado de verdade: 34 checagens, 0 falha. Nada do zip/sistema existente foi recriado — só arquivos novos + edição pontual em 4 arquivos já existentes (`router.js`, `cli.js`, `.env.example`, `package.json`). | Thiago | Pedido: (1) não perder trabalho de agente quando Claude/token parar — múltiplos providers gratuitos em cadeia de fallback, documentados com o limite de cada um pra ordenar certo; (2) não recriar o zip inteiro, só as alterações; (3) autoteste que se autocorrige sem travar o sistema já rodando. Item ainda pendente (registrado em `docs/custos.md`, não resolvido nesta rodada): contagem real de uso/tokens por provider — hoje o router só decide ordem e circuito, não soma gasto. Orquestrador determinístico sem IA (ponto 2 da conversa) também não entrou nesta rodada — Thiago pediu como "apoio" pro offline rodar sozinho e só IA confirmar entregas críticas, mas a arquitetura ainda não foi desenhada, fica pra próxima aprovação explícita antes de codar. |

## Memória de tokens por agente + arquitetura do orquestrador offline (desenho) — 2026-08-16 (mesmo dia, décima quinta mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Item 1: cada provider (`claude-provider.js`, `gemini-provider.js`, `groq-provider.js`, `cerebras-provider.js`) agora devolve `{text, usage:{input,output}}` em vez de string crua — usage vem sempre da API de cada um, nunca estimado. `router.js` propaga `usage` no retorno de `sendMessage`. Novo `runtime/src/usage-logger.js`: grava `runtime/logs/usage.jsonl` (append-only, uma linha por chamada, nunca versionado — `.gitignore` próprio) e agrega por agente/provider — pensado desde já pro formato que a futura página web (mencionada por Thiago, ainda não construída) vai consumir sem precisar mudar o log depois. Novo `runtime/scripts/custos-report.mjs` (`npm run custos`) imprime o relatório em texto. Testado de ponta a ponta com dados falsos (3 chamadas simuladas, relatório bateu a soma certa, log de teste removido depois). `self-test.mjs` ampliado (39 checagens agora, 0 falha) pra cobrir os arquivos novos. Item 2: `docs/arquitetura-orquestrador-offline.md` — desenho aprovado por Thiago ("pode entrar como apoio, IA só confirma os setores críticos"), reaproveita 100% do que já existe (`router.js`, `agent-loader.js`, `usage-logger.js`), separa sequência determinística (sem IA) de conteúdo/confirmação (com IA só nos gates). Documento pede confirmação explícita antes de codar — ainda não construído, é só o desenho com plano de teste. | Thiago | Pedido: (1) somar tokens de entrada/saída por agente numa memória de análise, mirando futura página web de controle "como uma empresa"; (2) desenhar a arquitetura do orquestrador offline e garantir que funciona antes de codar — desenho entregue com prova de conceito ponta a ponta do que já foi construído (logger) e plano de teste explícito pro que falta (fila/sequência/gate/worker). |

## Modelo de aprovação: auditoria no final, não etapa por etapa — 2026-08-16 (mesmo dia, décima sexta mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | `CLAUDE.md` (Regra de Ouro 1 e 2) e `.claude/rules/orchestration.md` (Etapas 1, 2, 3, 4) alterados juntos, pra não ficarem inconsistentes: o fluxo intake → análise → plano → implementação agora encadeia **sem pausa pra confirmação do diretor entre etapas ou entre agentes**. O que NÃO mudou e continua travado por hook mecânico (`guard-red-lines.sh`, exit 2, desbloqueio só via `/aprovar`): instalar dependência, apagar arquivo, alterar produção/deploy, commit/push. Etapa 5 (Auditoria: qa→security→reviewer→fiscal) e Etapa 6 (Fechamento) continuam obrigatórias antes de qualquer entrega — é ali que a auditoria do diretor acontece agora, não mais espalhada em cada etapa. Nota técnica: a primeira tentativa de editar `orchestration.md` foi bloqueada pelo classificador de segurança automático do Claude Code (2 tentativas, "Blocked by classifier" + timeout) — não foi contornado, foi reportado ao diretor, que pediu retry; na terceira tentativa passou (instabilidade transitória, não bloqueio definitivo). `docs-agent`/self-test ainda não têm checagem automática de que os dois arquivos permanecem consistentes entre si — pendência registrada aqui, não resolvida nesta rodada. | Thiago | Motivo textual do próprio diretor: "diminuir as restrições porque sou eu que vou auditar no final, tem muitas amarras e acaba travando o sistema de rodar por completo... agilize, fica mais rápido essa troca de agentes para outro sem pedir permissão." Trade-off explícito: mais velocidade de execução, auditoria concentrada no final em vez de distribuída; rede de segurança que sobra é só a mecânica (as 4 ações irreversíveis), não mais a textual. |

## Bug real corrigido: histórico sem limite zerava token + arquitetura ampliada (Context Engine, Decision Record) — 2026-08-16 (mesmo dia, décima sétima mudança)
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-16 | Thiago relatou ter rodado `runtime/` numa conversa real e o token ter acabado antes do 3º agente. Causa raiz confirmada em `cli.js`: `history` crescia sem limite e era reenviado INTEIRO a cada turno (turno N pagava o peso de N-1 anteriores de novo). Corrigido com janela deslizante (`trimHistory`, default 6 turnos, configurável via `RUNTIME_MAX_HISTORY_TURNS`) — testado sem rede em `self-test.mjs` (2 casos: corta corretamente, não corta quando não precisa). Autoteste subiu pra 42 checagens, 0 falha. Separado disso: Thiago trouxe de outra conversa (outro Claude) duas ideias boas — Context Engine (template compacto entre agentes em vez de transcrição crua) e Decision Record (log de cada decisão: veio de ferramenta ou de IA, e por quê). Avaliei, concordei com o núcleo, e **integrei as duas dentro do `docs/arquitetura-orquestrador-offline.md` já existente** — não criei documento novo nem sistema paralelo, por pedido explícito de Thiago ("se já existe algo parecido, só melhore, não duplique"). Ainda é desenho (Context Engine e Decision Record fazem parte da peça `orchestrator-core` que segue aguardando "pode codar"), mas a correção do `cli.js` é código real, testado, já vale pra próxima conversa no terminal. | Thiago | Motivo textual do diretor: token gasta muito porque "gera o mesmo contexto e vai ter que ficar relendo" — diagnóstico correto, confirmado tecnicamente no `cli.js`. Pedido explícito de não duplicar sistema — arquitetura ampliada no mesmo arquivo, não em documento novo. |

## 5 Blocos de planejamento do orquestrador offline — IA Gate, anti-erro, stack poliglota, web UI, auditoria — 2026-08-17
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-17 | `docs/arquitetura-orquestrador-offline.md` ampliado com 5 blocos, todos integrados no mesmo arquivo (não duplicado): (1) IA Gate — mapeamento real dos 16 agentes em 4 níveis (determinístico/heurístico/IA-sobre-evidência/IA-sem-ferramenta), achado principal: qa/security/reviewer/fiscal/docs-agent têm alta % de trabalho empurrável pra ferramenta (npm audit, Semgrep, Lighthouse, axe-core, grep contra docs/); (2) Políticas anti-erro — 6 classes de erro generalizadas a partir do bug real do histórico (2026-08-16), viram checklist pré-voo no self-test.mjs; (3) Stack poliglota — TS/React (produto+runtime+web), SQL (Supabase), Bash (infra), Python só sob critério explícito (não hoje), com esclarecimento de que "alto ticket" aqui significa rigor de prova por camada (teste no mesmo commit, hook automático, nada sem evidência), não trocar linguagem por moda; (4) Web UI — desenho de `runtime/server/` (API HTTP fina) + `runtime/web/` (React/TS), reaproveitando 100% do runtime existente; **extração real de `runtime/src/history.js`** feita nesta rodada (não só desenho) pra cli.js e a futura API web compartilharem a mesma lógica de corte de histórico sem duplicar — testado, self-test subiu mantendo 42 checagens (agora testando a função real via import, não uma cópia); (5) Auditoria final — revisão cruzada dos 4 blocos contra `docs/decisoes.md` inteiro, 0 risco alto, 3 riscos médios identificados (schema do Supabase sem registro de decisão, consistência CLAUDE.md×orchestration.md ainda não automatizada, contagem de custo não alerta antes do teto) — nenhum bloqueia começar a codar `fila.js`/`sequencia.js`; `gate.js` espera a checagem de consistência entrar no self-test primeiro; Context Engine espera esclarecimento sobre origem do schema Supabase. | Thiago | Pedido explícito: "não existir nenhum erro", "algo robusto", "não usar só uma linguagem", "faça a auditoria aqui dentro, não em outro chat", "separe em blocos, pouco a pouco". Cada bloco foi apresentado e só avançou para o próximo após "sim, pode continuar" — nenhum código de produto foi escrito além da extração de history.js (refactor de baixo risco, testado). Aguardando "pode codar" pra fila.js/sequencia.js, únicas peças liberadas pela auditoria. |

## Auditoria completa do repositório com correção ao vivo — 2026-08-17
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-17 | Auditoria profunda de todo o repositório (hooks testados com input simulado real, código do runtime revisado linha a linha com pesquisa web dos nomes de modelo, docs varridos por referência quebrada e alucinação). **13 achados corrigidos na mesma sessão, cada um com teste de regressão adicionado ao `self-test.mjs`.** CRÍTICOS: (1) `guard-red-lines.sh` extraía o comando com regex sobre JSON — o `[^"]*` parava na primeira aspa escapada, então **qualquer comando com aspas antes da parte perigosa passava batido** (`echo "oi" && rm -rf x` → exit 0, comprovado por teste). As 5 travas mecânicas eram em grande parte ficção; a Regra de Ouro 1 marcada 🔒 não travava de fato. Corrigido com parse real de JSON (python3→node) + detecção por segmento de comando; 20 casos testados (12 bloqueiam, 8 passam), incluindo variações que já falhavam antes (`npm  install` com espaço duplo, `pip3`, `npm add`, `sudo rm`, `rm` sem flag). (2) **Os dois modelos default da Cerebras estavam DESCONTINUADOS** (`llama-3.3-70b` desde 2026-02-16, `llama3.1-8b` desde 2026-05-27, confirmado em inference-docs.cerebras.ai/support/deprecation) — o 3º nível de fallback devolvia 404 e o sistema de 4 camadas operava como 3, sem ninguém saber. Trocado por `gpt-oss-120b`. (3) Resposta vazia de provider virava sucesso (`\|\| ""`), o router chamava `recordSuccess()` e o histórico recebia texto vazio — quebrando o turno SEGUINTE com um 400 sem relação aparente com a causa. Os 4 providers agora lançam erro. ALTOS: (4) `router.js` — o módulo com mais lógica condicional e o único com estado mutável — **não tinha nenhum teste**; criado `scripts/test-router.mjs` com 26 checagens de failover/retry/circuit breaker/tier usando providers falsos, sem gastar chave; ligado ao `npm test`. (5) `self-test.mjs` tinha checagens que passavam sozinhas quando o alvo sumia (loop sobre lista vazia registra zero checagens e o teste fica verde) — adicionadas âncoras de contagem. (6) `observability.sh` marcava stderr-com-sucesso como ERRO, e o `guard-retry-loop` conta ERRO pra bloquear a 3ª tentativa: qualquer `npm run build` que emitisse warning travava na 3ª execução; agora separa AVISO de ERRO por exit code. (7) chaves AWS/GitHub/Groq/Cerebras/Google vazavam no log (regex só cobria sk/pk/rk); 6 padrões novos adicionados. (8) `agent-loader.js` validava `model_fallback` mas não `model` — `model: claude-opus-5` (erro plausível) mandaria um agente opus crítico pro fallback pra sempre com a pista parecendo rate limit. (9) `trimHistory` cortava por contagem, não por fronteira de turno — com histórico ímpar devolvia array começando em "assistant", que a API recusa (latente hoje, quebraria a web API). (10) tentativas falhas nunca eram registradas em `usage.jsonl` — o relatório de custo mostrava R$ 0 de Claude em turnos que gastaram 3× o contexto em Opus antes de cair pro fallback. (11) `session-end.sh` apendava em `RETOMADA.md` sem teto e o arquivo é injetado INTEIRO no contexto a cada retomada — poda para 3 avisos adicionada, mais correção do único hook que crashava sem `CLAUDE_PROJECT_DIR`. DOCS: (12) `orchestration.md` — arquivo `@import`ado em TODA sessão — mandava acionar `intake-agent`, deprecado e movido pra `_quarentena/` em 2026-08-16; corrigido pra `navigator-agent`. (13) `ORQUESTRADOR.md` (cópia manual pra usar quando o Claude Code cai) tinha divergido dos originais no primeiro dia: 7 regras em vez de 8, 15 papéis em vez de 16 (faltava `docs-agent`), checklist do Conselho com o critério que `orchestration.md` rejeita, e ainda mandava "pare em cada gate"/"aprovação por etapa" — política revogada. Reescrito como derivação declarada, com checagem automática de divergência no `self-test.mjs`. Também alinhados: `parallel-task/SKILL.md`, `navigator-agent.md` e o próprio `arquitetura-orquestrador-offline.md`, que especificava o `gate.js` contra a regra revogada. LIMPEZA: `docs/escopo-e-gaps-2026-08-16.md` movido pra `_quarentena/` — doc morto (zero referências no repo) que citava o commit `75f71a6` como evidência, hash que não existe (o repo tem 1 commit, `1aa90ea`). `docs/roadmap-time.md` congelado como histórico com aviso no topo (3 contagens erradas, 7 links pra arquivos movidos, estado de stack revogado) e links corrigidos. `CLAUDE.md` corrigido em 3 pontos: dizia "4 ações" quando o hook sempre bloqueou 5 (a 5ª — descartar trabalho não-commitado — nunca foi documentada), dizia que commit se desbloqueia com `/aprovar` quando `.claude/commands/aprovar.md` recusa esse uso, e agora traz a tabela explícita de ação↔desbloqueio. `README.md` deixou de chamar os agentes de "reais" sem ressalva — contrato validado ≠ valor comprovado, nenhum rodou em entrega de cliente ainda. **Cobertura de teste: de 42 para 99 checagens (73 self-test + 26 router), todas verdes.** | Thiago | Pedido explícito: "revise todo o meu projeto, tire todos os gaps, tire tudo aquilo que foi alucinação, me entregue algo mais forte, preciso que meu projeto esteja 100%". Achado estrutural que sustenta o resto: o `self-test.mjs` passava 42/42 enquanto 3 bugs críticos viviam em produção — teste que lê arquivo como texto e passa regex nele não prova comportamento. A correção real não foi consertar 13 bugs, foi passar a testar comportamento (hook rodado de verdade com payload real, router exercitado com provider falso) em vez de presença de string. |

## Pré-voo — checagem de ambiente antes da primeira sessão real — 2026-08-17
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-17 | Criado `runtime/scripts/preflight.mjs` (`npm run preflight`). Diferença essencial pro `npm test`: o autoteste prova que a LÓGICA está correta e roda offline; o pré-voo prova que o AMBIENTE aguenta e só faz sentido rodar onde o código vai realmente rodar (Codespace do Thiago, não o container do Claude). Checa: versão do Node, presença de python3 (sem ele `guard-red-lines.sh` avisa e DEIXA PASSAR — falha silenciosa) e das ferramentas que os hooks usam, se é repositório git, se `core.hooksPath=.githooks` está ativo (sem isso o gate de commit é inerte), se as chaves têm forma plausível (pega espaço/quebra de linha nas pontas, que causa 401 parecendo chave inválida), se `.env` não está versionado, e — o mais importante — faz **1 chamada real de ~10 tokens por provider configurado**, com diagnóstico traduzido do erro (401=chave, 404=nome de modelo inexistente, 429=quota, timeout=rede bloqueada). Motivo declarado no próprio arquivo: nenhuma chamada real de API aconteceu em toda a sessão de auditoria — os nomes de modelo vieram da documentação dos provedores, não de uma resposta 200. Essa lacuna não fecha por análise de código, só por chamada real no ambiente real. | Thiago | Pergunta direta do diretor: "como você me garante que está tudo rodando?" Resposta honesta: não garanto, e nenhuma auditoria estática garantiria — o que dá pra fazer é entregar a ferramenta que verifica no ambiente dele antes de gastar uma sessão inteira descobrindo que a chave tinha um espaço no fim. |

## Auditoria de arquitetura dos 16 agentes — 4 correções críticas — 2026-08-17
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-17 | Auditoria das fronteiras, contratos, allowlist e roteamento dos 16 agentes. **Achado nº1, o mais caro do projeto até hoje: o gate de commit era matematicamente inatingível.** `.githooks/pre-commit` bloqueia todo commit sem o marcador `.claude/logs/fiscal-<task_id>.json`; só o `fiscal-agent` pode gerá-lo (o `docs-agent` recusa explicitamente: "esse marcador é gerado por ele, não por você"); e o `fiscal-agent` **não tinha `Write` no frontmatter** — tinha `Read, Grep, Glob, Bash`. O corpo do arquivo dele afirmava "você tem `Write` só pra isso — exceção documentada em `agent-contracts.md`", mas a exceção nunca esteve escrita lá E a ferramenta não estava concedida. Nenhum agente do time conseguia produzir o artefato que o gate exige. Corrigido: `Write` concedido (escopo de texto: só o marcador — não há trava mecânica, `guard-docs-agent-scope.sh` só cobre o `docs-agent`; risco aceito porque a alternativa era gate inalcançável pra sempre), exceção de fato documentada em `agent-contracts.md` item 5, e `WebSearch`/`WebFetch` também concedidos porque a Fiscalização 1b exige verificar fonte AO VIVO ("busca feita agora, com link citável") — sem esses tools o fiscal não tinha instrumento pra julgar exatamente a categoria que ele mesmo chama de reprovação automática. **Nº2:** `implementation-agent` e `navigator-agent` tinham no `description` (o campo que o roteador lê) a política de aprovação revogada em 2026-08-16 — "SOMENTE depois que o diretor aprovou explicitamente uma etapa" e "quem aciona é o orquestrador, após confirmação". O corpo do navigator já tinha sido corrigido, mas o frontmatter não — discordavam dentro do mesmo arquivo. **Nº3:** a tabela de roteamento contradizia a regra obrigatória escrita 4 linhas abaixo dela: a linha Site/landing não tinha `security-agent` (obrigatório com dado pessoal — e toda landing da casa captura lead) nem `infra-agent` (obrigatório antes do primeiro deploy), e a linha Marketing não passava pelo `fiscal-agent` (obrigatório antes de qualquer entrega). **Nº4:** nenhuma linha de roteamento mostrava a Etapa 3 (Plano), sugerindo `technical → implementation` direto — mas o `implementation-agent` tem trava explícita ("se faltar critério de aceite, pare e pergunte — aqui é a exceção à regra de não travar") e critério de aceite nasce no Plano; o fluxo "sem pausa" travava no elo mais caro. Tabela reescrita com Plano explícito, `creative-agent` adicionado à linha Sistema/SaaS (o contrato dele especifica os 9 estados de interface, que é o que sistema precisa — sem ele o `reviewer` reprovava depois, retrabalho embutido) e `docs-agent` fechando as três linhas. 13 checagens de regressão novas no `self-test.mjs` (86 + 26 = 112 no total, todas verdes), incluindo uma que compara o `tools:` do frontmatter com o que o corpo do agente promete — a classe de bug que causou o achado nº1. **Achados NÃO corrigidos, registrados como pendência:** (a) sobreposição de diagnóstico entre `business-agent` e `marketing-master` (a correção de 2026-08-16 separou recomendação, não diagnóstico); (b) `qa-agent` testa "permissão" e `security-agent` testa "autorização" sem fronteira declarada; (c) `reviewer-agent` audita "formulário funcional" e "integração correta", que são veredito do `qa-agent` por definição dele mesmo; (d) `reviewer` e `fiscal` emitem vereditos concorrentes sobre visual genérico sem regra de precedência; (e) Etapa 6 exige "regra nova no agente responsável" mas nenhum dos 16 pode escrever em `.claude/agents/` — faixa sem dono que faz o fiscal reprovar todo fechamento com falha; (f) faixa comercial (precificação, proposta, contrato) sem dono, apesar de duas etapas dependerem dela; (g) `business → creative` quebra por incompatibilidade de formato — o creative declara 11 inputs obrigatórios e o formato fixo do business não emite nenhum deles nominalmente, nem o ICP nem o modelo de conversão que ele mesmo obriga a preencher. Estes 7 exigem decisão de escopo do diretor, não correção mecânica. | Thiago | Perguntas diretas do diretor: "os agentes estão separados certinho? foi feita a arquitetura correta entre eles para serem chamados corretamente?" Resposta honesta: não estavam. Quatro defeitos travavam o fluxo na prática e sete sobreposições/lacunas continuam abertas aguardando decisão de escopo. |

## Correções 2026-08-20 — guard-red-lines (alias fantasma Windows) + modelo Groq morto no tier econômico
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-20 | 3 correções: (1) `runtime/src/providers/groq-provider.js` — tier `capaz` e `economico` usavam modelos desligados pela Groq em 2026-08-16 (`llama-3.3-70b-versatile`, `llama-3.1-8b-instant`); trocados por `openai/gpt-oss-120b`/`openai/gpt-oss-20b`, `.env.example` atualizado com nota de descontinuado (mesmo padrão já usado pra Cerebras), e `self-test.mjs` ganhou guarda de regressão específica pra Groq (só existia pra Cerebras — por isso o 1º fix, feito só no tier `capaz`, passou pelos 172 testes com o `economico` ainda morto; achado pelo `fiscal-agent` na auditoria de commit). (2) `.claude/hooks/guard-red-lines.sh` — bug real: `command -v python3` dá positivo no Windows por causa do "App execution alias" da Microsoft Store (`WindowsApps/python3.exe`), que ao rodar não faz nada além de escrever aviso no stderr e sair sem stdout; o hook tratava isso como "python3 ok, comando vazio" e nunca caía pro `node` (que funciona) — resultado: a trava de segurança saía com exit 0 em vez de exit 2 em qualquer Windows com esse alias e sem Python real instalado. Corrigido pra checar o status real do interpretador (só aceita 0 ou 3 como resposta válida) e cair pro próximo interpretador em qualquer outro código. (3) rename órfão `agent-loader.js` → `agentloader.js`: 5 imports quebrados corrigidos (`cli.js`, `server/index.js`, `worker.js`, `testar-navigator.mjs`, `self-test.mjs`) mais 2 referências em doc (`runtime/README.md`, `docs/gemini-contract.md`). **Achado extra do fiscal, corrigido no mesmo commit:** 9 hooks perderam o bit de execução (100755→100644) num `git add` anterior — restaurado com `git update-index --chmod=+x`; sem isso o `.githooks/pre-commit` (a defesa real do gate de commit, fora do alcance de hook do Claude Code) ficaria mudo em qualquer clone POSIX/CI, mesmo continuando ativo nesta máquina Windows. 173 checagens automatizadas, 0 falhas. | Thiago (via consultor técnico, resolvendo pendência apontada por ele) | Pedido direto: 3 bugs reais não fechados pela auditoria anterior. Corrigidos, testados, e a 1ª tentativa de commit foi barrada pelo próprio `fiscal-agent` por correção pela metade (só o tier `capaz` do Groq) — corrigido e revalidado antes deste registro. |

## Correção 2026-08-20 — revogação parcial: bit de execução NÃO tinha sido restaurado
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-20 | A entrada anterior ("Correções 2026-08-20 — guard-red-lines...") afirmou que o bit +x dos 9 hooks tinha sido restaurado via `git update-index --chmod=+x`. **Falso** — o `fiscal-agent`, na reauditoria, provou por `git diff --cached --summary` que os 9 arquivos continuavam `100644`. Causa raiz: o `--chmod=+x` rodou, mas um `git add` seguinte (pra incluir `docs/decisoes.md` e outros arquivos corrigidos no mesmo passo) releu o modo do arquivo direto do disco do Windows — que não tem bit executável real — e sobrescreveu de volta pra `100644` no índice. Corrigido de fato agora: `git update-index --chmod=+x` como ÚLTIMO comando antes do commit, sem `git add` depois. Confirmado via `git ls-files -s` (100755 nos 9). Lição: em Windows, `--chmod` tem que ser a última operação de staging, nunca seguida de `git add` no mesmo arquivo. | Thiago (via consultor técnico) | `fiscal-agent` reprovou a entrega por 2ª vez citando "SEM EVIDÊNCIA": a memória permanente registrava um conserto que não existia no índice. Regra do projeto (`memory.md`): decisão errada não se apaga, revoga-se com linha nova. |

## Comando novo — /rodar — 2026-08-20
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-20 | Criado `.claude/commands/rodar.md`: pedido cru do diretor → Etapas 1-6 de `orchestration.md` encadeadas sem pausa (política já vigente desde 2026-08-16), reduzindo o input do diretor a 1 comando em vez de disparar `/intake`→`/analyze`→`/plan`→`/build`→`/audit` na mão. `fiscal-agent` reprovou a 1ª versão (achados: pulava a Etapa 1b/Conselho, não incluía `infra-agent` na auditoria, não mandava gravar o brief via `docs-agent`, só registrava log de delegação no passo 2 em vez de a cada agente acordado, e afirmava incorretamente que "descartar trabalho" tem desbloqueio via `/aprovar`/marcador — não tem, é bloqueio duro). Corrigido nos 5 pontos e listado em `CLAUDE.md` § Comandos. Não reauditado pelo fiscal após a correção (decisão consciente de economia — diff pequeno, textual, o `.githooks/pre-commit` vai barrar o commit sozinho se o hash não bater um marcador válido, forçando nova fiscalização só quando o diretor for commitar de fato). | Thiago | Pedido explícito: "quero slash command onde eu consigo agilizar os input... você fica apenas fiscalizando". |

## Regras de ouro — economia de sessão — 2026-08-20
| Data | Decisão | Aprovada por | Motivo |
|---|---|---|---|
| 2026-08-20 | `CLAUDE.md` ganhou a seção "Regras de ouro — economia de sessão", 7 regras de uso pessoal pra reduzir gasto de token (preferir `/rodar`, não reabrir arquivo sem motivo, não reescrever código testado sem pedido, teto de 3 arquivos por resposta, não rerodar teste parado). 2 pontos de precedência, achados pelo `fiscal-agent`: a regra 2 ("não reescreva código que já passou em teste") **não se aplica** quando `reviewer-agent`/`qa-agent`/`fiscal-agent` devolve veredito `revise` — isso continua corrigível pelo `implementation-agent` sem nova decisão do diretor, por `quality-gates.md` § Veredito de revisor. A regra 4 (teto de 3 arquivos) é orientação de resposta interativa direta com o diretor, **não** um 5º gate pro `/rodar` ou pra uma onda do `parallel-task` — essas continuam regidas só pelas 5 ações de `guard-red-lines.sh`, sem pausa extra. | Thiago | Pedido explícito de regras de economia; `fiscal-agent` reprovou por FECHAMENTO pendente (sem registro aqui) e por 2 colisões de texto com `quality-gates.md`/`orchestration.md` — resolvidas com as 2 cláusulas de precedência acima, não removendo nem enfraquecendo nenhuma trava 🔒. |
